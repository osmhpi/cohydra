class Interface:

    def __init__(self, network, nodes, delay='0ms', speed='100Mbps'):
        self.network = network
        self.nodes = nodes
        self.delay = delay
        self.speed = speed
