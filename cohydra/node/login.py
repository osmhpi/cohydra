"""Login for authenticate with a registry."""

class Login:

    def __init__(self,username,password,registry):
        self.username = username
        self.password = password
        self.registry = registry
